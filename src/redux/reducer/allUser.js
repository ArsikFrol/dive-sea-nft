const defaultAllUser = {
    user: []
}

const allUser = (state = defaultAllUser, action) => {
    switch (action.type) {

        default:
            return { ...state }
    }
}

export default allUser